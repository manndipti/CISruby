
<table>
   
   <br   />
    <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
    <td>
    <%= image_tag(product.image_url, class: 'list_image') %> </td>
     
     <% if product == @current_product %>
     <td id="current_product" class="list_description"> 
     <% else %>
     <td class="list_description"> 
      <% end %>
      <dl>
      
        <dt><%= product.caterer_name %></dt>
        <dt><%= product.cuisine %></dt>
        <dt><%= product.product_title %></dt>
        
        
        <dd><%= truncate(strip_tags(product.description),
                 length: 80) %></dd>
        
    </td>
        <td class="list_actions">
        <td><%= link_to 'Show', product, class: 'btn btn-primary btn-xm' %></td>
      
             <td> <%= link_to "Edit", edit_product_path(product), :class => "btn btn-info btn-xm ", :remote => true, "data-toggle" => "modal", "data-target" => "#my-modal" %>

   <div class="modal fade" id="my-modal" title="My modal">

     <div class="modal-dialog">
        <div class="modal-content">

  <div class="modal-header">
    <button aria-hidden="true" class="close" data-dismiss="modal" type="button">×</button>
    <!--<h3 id="myModalLabel">New Product</h3> -->
  </div>
  <div id="product_modal" class="modal-body a-unique-class">
    
  </div>
  
</div>
</div>
</div>
</td>
        
        <td><%= link_to 'Delete', product, method: :delete, data: { confirm: 'Are you sure?' }, 
               class: 'btn btn-danger btn-xm', remote: true %></td>
      </tr>
 
</table>
